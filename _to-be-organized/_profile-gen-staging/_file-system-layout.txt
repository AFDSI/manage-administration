E:
    /users
        /gigster {on Windows this is C:\users\owner}
            .cache (directory)
            .config (directory)
            .local (directory)
            .nvm (directory)
            .secrets (directory)
            .ssh (directory)
            .uv (directory)
            .vscode (directory)
            .bash_history (file)
            .bashrc (file)
            .bashrc.generated (file)
            .env.secrets.nix (file)
            .env.secrets.ps1 (file)
            .profile (file)
            .zshrc (file)
        
            /workspace
                .claude (directory)
                .local (directory)
                .npm (directory)
                .nvm (directory)
                .pyenv (directory)
                .tmp (directory)
                .uv (directory)
                .vscode (directory)
                .bash_history (file)
                .bashrc (file)
                .claude.json (file)
                .claude.json.backup (file)
                .sudo_as_admin_successful (file)
                /dev
                /project
                /repos
