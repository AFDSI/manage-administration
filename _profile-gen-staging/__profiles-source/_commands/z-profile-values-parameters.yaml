# In profile-values.yaml
interface:
  # ... command_reference remains simple ...

  examples_reference:
    title: "Usage Examples & Snippets"
    sections:
      # --- This is a "Cookbook" style entry ---
      - heading: "Common Git Tasks"
        examples:
          - { title: "Amend the last commit without changing the message", snippet: "git commit --amend --no-edit" }
          - { title: "Revert a file to the last commit's version", snippet: "git checkout HEAD -- <file_path>" }

      # --- This is the new "Parameter Breakdown" style entry ---
      - heading: "Detailed Command: Get-S3Bucket (PowerShell)"
        command: "Get-S3Bucket"
        description: "A tool to list S3 buckets and their contents."
        parameters:
          - { flag: "-Region <name>", desc: "Specifies the AWS region to target (e.g., 'us-east-1')." }
          - { flag: "-BucketName <name>", desc: "(Optional) The name of a specific bucket to list." }
          - { flag: "-ListObjects", desc: "(Switch) If present, also lists the objects within the bucket(s)." }

